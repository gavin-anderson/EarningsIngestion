# Normalize line endings to LF in the repo; checkout as-is on all platforms.
* text=auto eol=lf

# Shell scripts must always be LF or they break in bash/sh on Linux/Mac.
*.sh text eol=lf

# Binaries — no line-ending conversion.
*.exe binary
go.sum -text
